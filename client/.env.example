# Eye Client Environment Configuration (Optional)
# In development, Vite automatically proxies /api and /socket.io requests to http://localhost:5050
# If deploying separately, specify the backend API base URL below:

# VITE_API_URL=http://localhost:5050
